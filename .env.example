# Environment Variables
VITE_API_URL=http://localhost:4000/api/v1
VITE_RAZORPAY_KEY=rzp_test_xxxxxx
VITE_STRIPE_KEY=pk_test_xxxxxx


Add a server endpoint to mark appointment as attended and wire the Attend button to call it.
Replace the alert in Attend action with a navigation to the meeting link (if meetingLink exists).
Add unit tests for the classification logic.
Make the "Attend Now" window configurable (prop or env).


update the appointmentsitem component :
show the client name instead of lawyer id and add the reschedule and connect button when its in the upcomming but when its in the attended add three buttons agreement, discussion and 